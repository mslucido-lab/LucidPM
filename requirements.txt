# Pinned from a clean install verified on 2026-08-16 (Python 3.12.10, Windows).
# Regenerate with `pip freeze > requirements.txt` from an activated .venv after
# any deliberate, tested dependency change -- do not casually re-freeze without
# re-running the boot check below, several of these pins are load-bearing:
#
# - reflex==0.8.9, NOT latest: app code uses `rx.Base` (14 live files in
#   LucidPM/pages/), removed in Reflex 0.9.0 in favor of Pydantic v2/
#   dataclasses. Migrating those files is real application-code work, out of
#   scope for this foundation session -- see docs/DeveloperSetup.md.
# - pydantic<2.11 + sqlmodel==0.0.24: reflex 0.8.9's pydantic-v1 compat shim
#   breaks under pydantic>=2.11 (ImportError: PydanticDeprecatedSince211), but
#   sqlmodel>=0.0.39 *requires* pydantic>=2.11. sqlmodel 0.0.24 is the version
#   contemporaneous with reflex 0.8.9's own release and doesn't have that floor.
#
# Even with these pins, `reflex run` does NOT fully boot yet -- see
# Undelivered Handoffs/LucidoPM_ChatGPT_Handoff_49_UploadHandlerCompat.md for
# a separate, unrelated app-code issue (not a dependency problem) blocking the
# tenants and communications pages specifically.

alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anthropic==0.122.0
anyio==4.14.2
bidict==0.23.1
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.4.2
colorama==0.4.6
cryptography==50.0.0
distro==1.9.0
docstring_parser==0.18.0
fastapi==0.141.1
granian==2.8.1
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jiter==0.16.0
Mako==1.4.1
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
numpy==2.5.2
packaging==25.0
pandas==3.0.5
pillow==12.3.0
platformdirs==4.11.3
psutil==7.2.2
pycparser==3.0
pydantic==2.10.6
pydantic_core==2.27.2
Pygments==2.20.0
pyodbc==5.3.0
pypdf==6.16.1
python-dateutil==2.9.0.post0
python-engineio==4.13.5
python-multipart==0.0.32
python-socketio==5.16.4
redis==6.4.0
reflex==0.8.9
reflex-hosting-cli==0.1.69
reportlab==5.0.0
rich==14.3.4
simple-websocket==1.1.0
six==1.17.0
sniffio==1.3.1
SQLAlchemy==2.0.52
sqlmodel==0.0.24
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
tzdata==2026.3
uvicorn==0.52.3
watchfiles==1.2.0
wrapt==1.17.3
wsproto==1.3.2
